[project]
name = "thunderbird-ai-pipeline-lab"
version = "0.1.0"
description = "Deterministic evaluation lab for Thunderbird's generic AI pipeline"
requires-python = ">=3.14"
license = "MPL-2.0"

[tool.unittest]
start-directory = "tests"

